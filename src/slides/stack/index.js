import html from './stack.html?raw'
import css  from './stack.css?inline'

export default {
  id:        'stack',
  narration: `Cheap simulation runs headless; high-fidelity rendering only spins up when a viewer tunes in. We're starting with a single open source, 24-hour interactive live stream — and the same architecture scales to thousands of channels.`,
  html,
  css,
}
