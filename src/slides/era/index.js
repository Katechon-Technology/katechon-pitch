import html from './era.html?raw'
import css  from './era.css?inline'

export default {
  id:        'era',
  narration: `The first era of the internet was built around the HTML page. Social media was built around the video and photo file. The next era will be built around the browser itself — streamed as an overlay, generated in real time.`,
  html,
  css,
}
